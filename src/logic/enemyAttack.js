//takes in arrays of previously selected coords (separated into hits and misses) and returns a new location to attack
//also takes in location of last attack (to see whether or not the enemy should randomize the next attack location or try an adjacent)

export const enemyAttack = (prevHits, prevMisses, lastAttack) => {
  //do code
};
